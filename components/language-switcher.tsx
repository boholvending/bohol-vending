"use client";import{Globe2}from"lucide-react";import{usePathname,useRouter}from"next/navigation";
export function LanguageSwitcher({dark=true}:{dark?:boolean}){const path=usePathname();const router=useRouter();const isZh=path==="/zh"||path.startsWith("/zh/");function change(value:string){const clean=isZh?path.replace(/^\/zh/,"")||"/":path;if(value==="zh")router.push(`/zh${clean==="/"?"":clean}`);else router.push(clean)}return <label className={`language-switcher ${dark?"on-dark":""}`}><Globe2 size={16}/><span className="sr-only">Language</span><select value={isZh?"zh":"en"} onChange={e=>change(e.target.value)} aria-label="Choose language"><option value="en">EN</option><option value="zh">中文</option></select></label>}
